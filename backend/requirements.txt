# ================================================================
# Invoice Decision Tool — Backend Dependencies
# Python 3.12+ required
# Install: pip install -r requirements.txt
# ================================================================

# Web Framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database — PostgreSQL (local + Neon.tech on Replit)
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Settings / Environment Variables
pydantic[email]==2.10.3
pydantic-settings==2.7.0

# Auth — JWT + Password Hashing
python-jose==3.3.0
bcrypt==4.2.1

# AI / OCR
PyMuPDF==1.25.1
python-multipart==0.0.12   # Required for file uploads

# OpenAI — GPT-4o Vision extraction
openai==1.57.4

# Supabase — File storage
supabase==2.10.0
httpx==0.27.2              # Also used by supabase client
aiofiles==25.1.0
